commands:
  01_cleanup_disk:
    command: |
      journalctl --vacuum-size=100M
      find /var/log -name "*.log" -size +50M -exec truncate -s 0 {} \;
      rm -rf /tmp/*.jar 2>/dev/null || true
    ignoreErrors: true