commands:
  01_copy_native_libs:
    command: |
      echo "Copying native libraries to system library path..."
      find /var/app/current/src/main/java/com/app/main/root/app -name "*.so" -exec cp {} /usr/lib64/ \;
      ldconfig
      echo "Native libraries copied and ldconfig updated"
    ignoreErrors: false
